# Overrides for `make website_local` (Jekyll served from inside a container).
# Override locally if you serve from another host, e.g.
# `url: "http://<your-host>:4000"` — do not commit a machine-specific address.
url: "http://localhost:4000"
